# CLI

- `greplog init` — detect framework, establish service identity, write config.
- `greplog dev` — start local workspace agent + dashboard, print localhost URL. Exits the CLI process once the agent is healthy (runs in background unless `--foreground` is used).
- `greplog status` — show agent health, buffer size, disk usage.
