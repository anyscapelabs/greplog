# v0.1 Services Page — Product Design

## Page Purpose

The Services page gives devs a per-service health dashboard. Unlike Analytics (which shows aggregate cluster-wide metrics), Services is about **individual service status, health trends, and quick triage**. The existing analytics `ServiceHealthChart` shows a 30,000ft view — this page is the drill-down.

---

## Page Layout

```
┌──────────────────────────────────────────────────────────────┐
│ Grep Services                     [Refresh] [Live]           │
├──────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│ │ api       │ │ web       │ │ db        │ │ worker    │       │
│ │ ● Healthy │ │ ● Healthy │ │ ● Degraded│ │ ● Down   │       │
│ │ 99.8%     │ │ 99.2%     │ │ 87.5%    │ │ 52.1%    │       │
│ │ 1.2M req  │ │ 890k req  │ │ 450k req │ │ 120k req │       │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                                                              │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │
│ │ Requests     │ │ Error Rate   │ │ Avg Latency  │         │
│ │ by Service   │ │ by Service   │ │ by Service   │         │
│ │  (bar chart) │ │  (bar chart) │ │  (bar chart) │         │
│ └──────────────┘ └──────────────┘ └──────────────┘         │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Service Name │ Status  │ Uptime │ Reqs  │ Error │ P95   │ │
│ │──────────────│─────────│────────│───────│───────│───────│ │
│ │ ● api        │ Healthy │ 99.8%  │ 1.2M  │ 0.1%  │ 45ms  │ │
│ │ ● web        │ Healthy │ 99.2%  │ 890k  │ 0.3%  │ 52ms  │ │
│ │ ● db         │ Degraded│ 87.5%  │ 450k  │ 2.1%  │ 230ms │ │
│ │ ● worker     │ Down    │ 52.1%  │ 120k  │ 8.7%  │ 890ms │ │
│ │              │         │        │       │       │       │ │
│ │        [View] │ [View] │ [View] │ [View]│ [View]│ [View]│ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

---

## Section 1: Service Health Cards (top row)

Four cards in a row, one per service. Each card shows:

| Element | Description | Example |
|---------|-------------|---------|
| Service name | Bold label | `api` |
| Health dot | Color-coded circle | 🟢 Healthy / 🟡 Degraded / 🔴 Down |
| Uptime % | Percentage over selected window | `99.8%` |
| Request count | Total requests in window | `1.2M` |
| Mini sparkline | Request volume trend (reuse `AnalyticsMetricCard` pattern) | line chart |

**Health status thresholds:**
- **Healthy** — error rate < 1%, latency P95 < 100ms, uptime > 99%
- **Degraded** — error rate 1-5%, latency P95 100-500ms, uptime 90-99%
- **Down** — error rate > 5%, latency P95 > 500ms, uptime < 90%

---

## Section 2: Chart Row (3 side-by-side)

Reuse existing `AnalyticsChartPanel` wrapper pattern.

### Chart 1: Requests by Service
- **Type:** Horizontal bar chart (reuse `NoisyServicesChart` pattern)
- **X-axis:** Request count
- **Y-axis:** Service name
- **Color:** Blue, with bar colored by health status
- **Dropdown:** Metric — `Count | Rate`

### Chart 2: Error Rate by Service
- **Type:** Horizontal bar chart
- **X-axis:** Error rate %
- **Y-axis:** Service name
- **Color:** Red gradient (higher = darker)
- **Dropdown:** Metric — `Count | Rate`

### Chart 3: Avg Latency by Service
- **Type:** Horizontal bar chart
- **X-axis:** Latency in ms
- **Y-axis:** Service name
- **Color:** Amber gradient (higher = darker)
- **Dropdown:** Metric — `Avg | P50 | P95 | P99`

---

## Section 3: Services Table

Virtualized table (same pattern as `LogsTable` / `ErrorsTable`).

### Columns

| Column | Width | Type | Notes |
|--------|-------|------|-------|
| View | 60px | Button | Opens service detail drawer |
| Service | 150px | Name + health dot | e.g., `● api` |
| Status | 100px | Badge | Healthy / Degraded / Down (color-coded) |
| Uptime | 90px | % | Over selected time range |
| Requests | 110px | Number | Formatted (1.2M, 890k) |
| Error Rate | 100px | % | With trend arrow |
| Avg Latency | 100px | ms | |
| P95 Latency | 100px | ms | |
| P99 Latency | 100px | ms | |
| Last Seen | 150px | Relative time | "2m ago", "1h ago" |

**Table toolbar:** Row count, Limit dropdown, Export button (same pattern as Logs)

---

## Section 4: Filter Sidebar

Collapsible left sidebar (same `FilterSection` pattern as Logs/Errors).

### Filter Sections

| Section | Items | Notes |
|---------|-------|-------|
| **health_status** | Healthy, Degraded, Down | With color dots and counts |
| **service_name** | api, web, db, worker | With log count per service |
| **environment** | production, staging (future) | Placeholder for v0.2 |

---

## Section 5: Service Detail Drawer

Right slide-in drawer (same UX as Log/Error detail drawers). Same close behavior: X button, ESC key, click outside.

### Full Wireframe

```
┌───────────────────────────────────────────┐
│ Service: api                       [X]    │  <- sticky header
│ 🟢 Healthy · 99.8% uptime · 2.5k req/s   │
├───────────────────────────────────────────┤
│                                           │
│  ── Performance Metrics ──                │
│  ┌────────┐┌────────┐┌────────┐┌────────┐│
│  │ Reqs   ││ Err %  ││ Avg ms ││ P95 ms ││  <- 4 mini metric cards
│  │ 1.2M   ││ 0.12%  ││ 45ms   ││ 95ms   ││     with sparkline
│  └────────┘└────────┘└────────┘└────────┘│
│                                           │
│  ── Health Timeline ──                    │
│  🟢 🟢 🟢 🟡 🟢 🟢 🟢 🟢 🟡 🟢 🟢 🟢  │  <- colored dot timeline
│  12:00                    14:00    now    │     (custom component)
│                                           │
│  ── Service Details ──                    │
│  Service ID:    svc_a1b2c3d4              │  <- copyable
│  Environment:   production                │
│  Host:          api-01, api-02, api-03    │
│  Version:       v2.14.1                   │
│  Uptime:        99.8%                     │
│  Last deployed: 2h ago                    │
│  First seen:    2026-01-15                │
│                                           │
│  ── Recent Errors (5) ──                  │
│  ┌──────────────────────────────────────┐│
│  │ 14:23:11 │ 503 │ Service unavailable ││  <- mini table, click row →
│  │ 14:22:05 │ 502 │ Bad gateway         ││     navigates to Errors page
│  │ 14:20:33 │ 408 │ Request timeout     ││     filtered to this service
│  │ 14:18:12 │ 500 │ Internal error      ││
│  │ 14:15:44 │ 429 │ Rate limit          ││
│  └──────────────────────────────────────┘│
│                                           │
│  ── Related Logs (last 5) ──              │
│  ┌──────────────────────────────────────┐│
│  │ 14:23:11 │ ERROR │ Connection pool.. ││  <- click row → navigates
│  │ 14:22:05 │ ERROR │ Upstream refused  ││     to Logs page filtered
│  │ 14:20:33 │ WARN  │ Slow query (2.3s)││     to this service
│  │ 14:18:12 │ ERROR │ Handler crashed   ││
│  │ 14:15:44 │ INFO  │ Health check OK   ││
│  └──────────────────────────────────────┘│
│                                           │
│  └─────────────────────────────────────── │
│  [View Logs →]  [View Errors →]  [View in Analytics →]  │  <- footer
└───────────────────────────────────────────────────────────┘
```

### Content Sections Detail

#### Header
- Service name (bold, large text)
- Health dot + status label (color-coded)
- Uptime %
- Request throughput (req/s)

#### Performance Metrics (4 mini cards)
Same visual pattern as the top-row ServiceCards but condensed into the drawer:

| Metric | Format | Source |
|--------|--------|--------|
| Total Requests | Number (1.2M) | `total_requests` |
| Error Rate | Percentage (0.12%) | `error_rate` |
| Avg Latency | ms (45ms) | `avg_latency` |
| P95 Latency | ms (95ms) | `p95_latency` |

Each card gets a mini sparkline (reuse `AnalyticsMetricCard` pattern).

#### Health Timeline (custom component)
Colored dot timeline showing service health at 5-min intervals over the selected time range:
- 🟢 Green dot = healthy interval (0 errors)
- 🟡 Amber dot = degraded (1-5% error rate)
- 🔴 Red dot = down (>5% error rate)
- Click dot → shows what error caused the degradation

#### Service Details (key-value list)
| Field | Example | Copyable? |
|-------|---------|-----------|
| Service ID | `svc_a1b2c3d4` | ✅ Yes (click to copy) |
| Environment | production | No |
| Hosts | api-01, api-02, api-03 | No |
| Version | v2.14.1 | No |
| Uptime | 99.8% | No |
| Last deployed | 2h ago | No |
| First seen | 2026-01-15 | No |

#### Recent Errors (mini table)
- Last 5 errors from this service
- Columns: timestamp, error_code, message
- Click row → navigates to Errors page with `service_name` pre-filtered
- Empty state: "No recent errors" with green checkmark

#### Related Logs (mini table)
- Last 5 log entries from this service
- Columns: timestamp, level, message
- Click row → navigates to Logs page with `service_name` pre-filtered
- Empty state: "No logs in this time range"

#### Footer Actions
```
[View Logs →]          → /logs?service=api
[View Errors →]        → /errors?service=api
[View in Analytics →]  → /analytics?service=api
```
Each navigates with the service pre-selected in the dropdown filter. Add `?service=` query param support to Logs, Errors, and Analytics pages.

### Required Backend Fields (for the drawer)

```json
{
  "id": "svc_a1b2c3d4",
  "name": "api",
  "status": "healthy",
  "environment": "production",
  "hosts": ["api-01", "api-02", "api-03"],
  "version": "v2.14.1",
  "uptime_pct": 99.8,
  "total_requests": 1200000,
  "error_rate": 0.12,
  "avg_latency_ms": 45,
  "p95_latency_ms": 95,
  "p99_latency_ms": 180,
  "last_deployed": "2026-07-18T12:00:00Z",
  "first_seen": "2026-01-15T00:00:00Z",
  "health_timeline": [
    { "timestamp": "2026-07-18T12:00:00Z", "status": "healthy" },
    { "timestamp": "2026-07-18T12:05:00Z", "status": "healthy" }
  ],
  "recent_errors": [
    { "timestamp": "2026-07-18T14:23:11Z", "error_code": 503, "message": "Service unavailable" }
  ],
  "recent_logs": [
    { "timestamp": "2026-07-18T14:23:11Z", "level": "error", "message": "Connection pool exhausted" }
  ],
  "request_timeseries": [/* ... */],
  "error_rate_timeseries": [/* ... */],
  "latency_timeseries": [/* ... */]
}
```

### Empty States
- **No recent errors:** Show green checkmark + "All healthy — no recent errors"
- **No related logs:** Show muted "No logs captured in this time range"
- **No timeseries data (new service):** Show flat line + "Collecting data..."

### Status-Dependent Rendering
| Status | Header Dot | Metric Emphasis | Error Section |
|--------|-----------|----------------|--------------|
| 🟢 Healthy | Green | Show all metrics normally | Minimal/compact — may be empty |
| 🟡 Degraded | Amber | Highlight error_rate + latency | Show expanded with first error cause |
| 🔴 Down | Red | Show all metrics; pulse animation on border | Show full table with stack traces

---

## Service Health Chart Type

The existing analytics `ServiceHealthChart` uses a **stacked horizontal bar** showing Healthy/Degraded/Down hours per service. This is the right chart type for the aggregate view.

For the **Services page**, add a **per-service health timeline** (in the drawer):

```
Service Health Over Time
─────────────────────────
 🟢 🟢 🟢 🟡 🟢 🟢 🟢 🟢 🟡 🟢 🟢 🟢  api
 🟢 🟢 🟢 🟢 🟢 🟡 🟡 🔴 🟡 🟢 🟢 🟢  web
 🟢 🟡 🔴 🟡 🟡 🟡 🟢 🟢 🟢 🟡 🟡 🟡  db
 🟡 🔴 🔴 🔴 🟡 🟡 🟡 🟡 🔴 🔴 🔴 🔴  worker
─────────────────────────
 12:00                    14:00         now
```

This is a **colored dot timeline** (not a standard chart — custom component):
- X-axis: time (last 2 hours, bucketed into 5-min intervals)
- Y-axis: each service
- Dots: colored by health status at that interval
- Click a dot to see the error that caused the degradation

---

## Required Backend Fields

**Per service:**
`name`, `status` (healthy|degraded|down), `uptime_pct`, `total_requests`, `error_rate`, `avg_latency`, `p95_latency`, `p99_latency`, `last_seen`, `request_volume_timeseries[]`, `error_rate_timeseries[]`, `latency_timeseries[]`

**Per service health event:**
`timestamp`, `service_name`, `status`, `error_id` (linkable), `duration_minutes`

---

## v0.1 Implementation Priority

1. **Service health cards** (top row) — Build a `ServiceCard` component
2. **Services table** — Virtualized table with sortable columns
3. **Filter sidebar** — Reuse existing `FilterSection` pattern with health_status filter
4. **Chart row** — 3 bar charts (reuse `NoisyServicesChart` / `AnalyticsChartPanel` patterns)
5. **Detail drawer** — Same drawer component from Logs/Errors, with service-specific sections
6. **Health timeline** — Custom colored-dot component (lowest priority for v0.1, acceptable as placeholder)
