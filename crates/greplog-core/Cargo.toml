[package]
name = "greplog-core"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
blake3 = "1"

[build-dependencies]
prost-build = "0.13"
